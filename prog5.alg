algoritmo "área"
var
l1, l2, resultado: inteiro

inicio
    escreval("Digite um lado do quadrado:")
    leia(l1)
    escreval("Digite outro lado do quadrado:")
    leia(l2)
    resultado <- l1 * l2
    escreval("Essa é a área do quadrado: ",resultado)
    fimalgoritmo